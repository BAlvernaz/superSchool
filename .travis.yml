matrix:
    include:
      - language: python
        python: 3.8
        services: postgresql
        beforeScript:
          - pip install -r requirements.txt
          - psql -c 'create database superSchool;' -U postgres
          - psql -c 'create role balvernaz superuser;' -U postgres
        script:
          - pytest
        
